import UIKit
import Then

class KGAbilityView: UIStackView {
    private let myAblitiLabel = UILabel().then {
        $0.text = "내 실력"
        $0.font = .killerGramFont(.semibold, style: .m2)
    }
    private let showMyAblituLabel = UILabel().then {
        
    }
    private let changeButotn = UIButton()
}
